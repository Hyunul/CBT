package com.example.cbt.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS configuration is handled in SecurityConfig via WebCorsConfig
}
